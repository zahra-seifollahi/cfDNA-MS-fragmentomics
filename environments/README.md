# Environment files

This folder contains files used to document and restore the computational environments used in this project.

## R environment

The R package versions are recorded in:

```text
environments/r/renv.lock
